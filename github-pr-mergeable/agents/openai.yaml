interface:
  display_name: "GitHub PR Mergeable"
  short_description: "Drive PRs through mergeable gates and merge when authorized/default"
  default_prompt: "Use $github-pr-mergeable to make a GitHub PR or PR stack mergeable and merge by default when acting as coordinator after gates pass: latest-head CI, tests, benchmarks, and AI review resolution."
