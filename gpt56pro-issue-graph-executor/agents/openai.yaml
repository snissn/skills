interface:
  display_name: "GPT-5.6 Pro Issue Graph Executor"
  short_description: "Advance issue graphs in parallel with resumable handoffs"
  default_prompt: "Use $gpt56pro-issue-graph-executor on this parent tracker to merge ready foundations, advance the widest safe independent wave through local builds and tests, and leave exact idempotent handoff state for every unfinished lane."
