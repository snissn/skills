interface:
  display_name: "Scientific Mainline Workflow"
  short_description: "Iterate on branches, freeze reviewed science to main"
  default_prompt: "Use $scientific-mainline-workflow to develop this scientific candidate on a pushed issue-scoped feature branch, bind independent review to exact candidate bytes, validate it locally, and integrate only the reviewed freeze onto synchronized main without PR churn."
