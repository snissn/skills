interface:
  display_name: "Scientific Mainline Workflow"
  short_description: "Direct-to-main scientific workflow with review and validation"
  default_prompt: "Use $scientific-mainline-workflow to construct this scientific result mutably, bind an independent audit to the exact candidate, validate it locally, and integrate one coherent disposition directly onto synchronized main without PR churn."
